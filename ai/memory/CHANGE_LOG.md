# CHANGE LOG

## 2026-04-20
- Se ejecuta `perfilar` del repositorio y se reemplaza `PROJECT_PROFILE.yaml` con un perfil operativo anclado a estructura real (`README`, `docs`, `pubspec`, `lib/`, `test/`, `tool/`).
- Se inicializa `PROJECT_MEMORY.md` con decisiones estables de arquitectura y restricciones recurrentes para futuras tareas.
EOF
